# ── IDS Backend — Lean Requirements (No TensorFlow) ──────────────────────────
# Install with:
#   cd D:\IDS_Project\backend
#   venv\Scripts\activate
#   pip install -r requirements.txt

# Core ML
scikit-learn>=1.3.0
xgboost>=2.0.0
onnxruntime>=1.17.0          # replaces TensorFlow — 15MB vs 600MB
numpy>=1.24.0
pandas>=2.0.0
joblib>=1.3.0
scipy>=1.11.0

# API server
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
websockets>=12.0
python-multipart>=0.0.9
pydantic>=2.0.0

# Packet capture (requires Npcap installed on Windows)
scapy>=2.5.0
